
# services:
#   backend:
#     build:
#       context: ./backend
#       dockerfile: Dockerfile
#     ports:
#       - "3000:3000"
#     volumes:
#       - ./backend:/app
#       - /app/node_modules
#     env_file:
#       - ./backend/.env
#     restart: always
#     command: npm run dev

#   frontend:
#     build:
#       context: ./frontend
#       dockerfile: Dockerfile
#     ports:
#       - "5173:80"
   
#     depends_on:
#       - backend
#     restart: always

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile  # ✅ Still using Dockerfile inside backend folder
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    restart: always
